# Copy this file to .env and fill in secure values. Do NOT commit real secrets to source control.
# Generate a secure SECRET_KEY, e.g. in Python: `python -c "import secrets; print(secrets.token_urlsafe(32))"`

SECRET_KEY=change-me-generate-a-secure-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_ALGORITHM=HS256
# DATABASE_URL 非必填，如果未配置 DATABASE_URL，默认使用 sqlite
DATABASE_URL=postgres://postgres:password@localhost:5432/postgres
